"import json\nimport os\n\nlog_path = \"/Users/alpha7en/.gemini/antigravity-cli/brain/88110c6a-259b-4bd3-8107-5a993318958e/.system_generated/logs/transcript.jsonl\"\nqueries = [\"qpe-2\", \"entanglement-swapping\", \"phase-flip-code\", \"qft-3\"]\n\nif os.path.exists(log_path):\n    with open(log_path, 'r', encoding='utf-8') as f:\n        for idx, line in enumerate(f):\n            # Skip if this is the search script itself\n            if \"search_log.py\" in line:\n                continue\n            if any(q in line for q in queries):\n                try:\n                    obj = json.loads(line)\n                    # Let's inspect content or tool calls\n                    content = str(obj.get(\"content\", \"\"))\n                    tool_calls = str(obj.get(\"tool_calls\", \"\"))\n                    \n                    # We are looking for lines where these tasks are written or replaced in TaskManager.tsx\n                    if \"TaskManager.tsx\" in line and (\"replace_file_content\" in line or \"write_to_file\" in line):\n                        print(f\"=== Match on Line {idx}, step {obj.get('step_index')} ===\")\n                        for q in queries:\n                            if q in tool_calls:\n                                print(f\"Found task '{q}' in tool call:\")\n                                # Print around the task definition\n                                pos = tool_calls.find(q)\n                                print(tool_calls[max(0, pos-200):min(len(tool_calls), pos+3000)])\n                                print(\"-\" * 50)\n                except Exception as e:\n                    pass\nelse:\n    print(\"Log not found.\")\n"