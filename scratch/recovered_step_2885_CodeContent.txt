"import json\nimport os\n\nlog_path = \"/Users/alpha7en/.gemini/antigravity-cli/brain/88110c6a-259b-4bd3-8107-5a993318958e/.system_generated/logs/transcript.jsonl\"\n\nqueries = [\"qpe-2\", \"entanglement-swapping\", \"phase-flip-code\", \"qft-3\", \"grover-diffuser-2\"]\n\nif os.path.exists(log_path):\n    print(\"Found transcript log.\")\n    with open(log_path, 'r', encoding='utf-8') as f:\n        for idx, line in enumerate(f):\n            if any(q in line for q in queries):\n                print(f\"--- Line {idx} contains match ---\")\n                try:\n                    obj = json.loads(line)\n                    # Let's print a small excerpt of tool calls or text content containing the match\n                    content = str(obj.get(\"content\", \"\"))\n                    tool_calls = str(obj.get(\"tool_calls\", \"\"))\n                    for q in queries:\n                        if q in content:\n                            pos = content.find(q)\n                            print(f\"Found '{q}' in step content around index {pos}:\")\n                            print(content[max(0, pos-200):min(len(content), pos+1500)])\n                            print(\"=\"*40)\n                        if q in tool_calls:\n                            pos = tool_calls.find(q)\n                            print(f\"Found '{q}' in step tool_calls around index {pos}:\")\n                            print(tool_calls[max(0, pos-200):min(len(tool_calls), pos+1500)])\n                            print(\"=\"*40)\n                except Exception as e:\n                    print(\"Error parsing line:\", e)\nelse:\n    print(\"Transcript log not found at\", log_path)\n"