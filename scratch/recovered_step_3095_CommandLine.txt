"python3 -c \"\nimport re\n\np_in = '/Users/alpha7en/Documents/development/quantum_app/scratch/qpe_search_results.txt'\ncontent = open(p_in, 'r', encoding='utf-8').read()\n\n# Let's search for tasks using regex.\n# Tasks usually look like: { id: '...', ... difficulty: '...', explanationMarkdown: `...` }\n# We can find blocks that contain id: 'qpe-2' and go until a matching closing bracket or a reasonable limit.\n\ndef find_task(task_id):\n    # Search for id: 'task_id' or id: \\\"task_id\\\"\n    matches = []\n    # Find all start indices\n    for m in re.finditer(r'id:\\s*[\\'\\\"]' + re.escape(task_id) + r'[\\'\\\"]', content):\n        start_idx = m.start()\n        # look back to find the opening '{'\n        # search backward up to 500 chars\n        look_back = content[max(0, start_idx - 500):start_idx]\n        brace_pos = look_back.rfind('{')\n        if brace_pos != -1:\n            task_start = max(0, start_idx - 500) + brace_pos\n        else:\n            task_start = start_idx\n        \n        # Now find the matching closing brace, or look forward 3000 chars\n        sub = content[task_start:task_start + 4000]\n        # count braces\n        brace_count = 0\n        end_pos = -1\n        for i, char in enumerate(sub):\n            if char == '{':\n                brace_count += 1\n            elif char == '}':\n                brace_count -= 1\n                if brace_count == 0:\n                    end_pos = i\n                    break\n        if end_pos != -1:\n            task_code = sub[:end_pos + 1]\n        else:\n            task_code = sub[:1500] + '\\n...[TRUNCATED]...'\n        matches.append((task_start, task_code))\n    \n    print(f'=== FOUND {len(matches)} MATCHES FOR {task_id} ===')\n    for idx, (pos, code) in enumerate(matches):\n        print(f'Match {idx} at position {pos}:')\n        print(code)\n        print('-'*50)\n\nfind_task('qpe-2')\nfind_task('entanglement-swapping')\n\""